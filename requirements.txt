# Core
psutil>=6.0.0
pydantic>=2.5.0
structlog>=25.4.0
fastapi>=0.100.0
uvicorn>=0.22.0
Flask>=3.0.0
httpx>=0.24.0
numba>=0.59.0
python-dotenv>=1.0.0

# Replace: nvidia-ml-py>=12.535.133
# With:
nvidia-ml-py>=12.555.43  # Use new package name
# pynvml is now a wrapper around nvidia-ml-py, so either works

# MCP
mcp>=0.1.0

# GPU Detection
# (nvidia-ml-py handled above)

# TUI
textual>=0.52.0

# Data
numpy>=1.24.0
pyyaml>=6.0.1
huggingface-hub>=0.20.0
gguf>=0.6.0

# Terminal UI Utilities
rich>=13.3.0
questionary>=1.10.0
customtkinter>=5.2.0
pillow>=10.0.0
lmstudio>=1.5.0
